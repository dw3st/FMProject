// Re-exported from Domain so backend callers don't need path changes during migration.
export type { MetaRef } from "@/Domain/clubLookup";
export { isPlayerSquad, isPlayerSquadId, findPlayerSquad } from "@/Domain/clubLookup";
