node_modules
dist
.DS_Store
.env

# Final rendered MP4 outputs.
out

# Still-frame screenshots generated during scene verification.
preview

# Large source video clips used by Remotion via staticFile(). Recordings
# captured from /promo (lab server) live here locally but should not be
# committed — too large for git. Re-record them via the lab server when
# rebuilding the promo from scratch:
#   bun run lab
#   open http://localhost:4000/promo?scenario=el-clasico
# then screen-capture into public/{fail,shot_miss,goal}.mov.
public/*.mov
public/*.mp4
public/*.webm
public/*.mkv
