
[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"

[test]
# Isolates RUNTIME_DATA_DIR to a disposable temp dir before any test file (or its imports) load —
# see scripts/testPreload.ts for why this matters.
preload = ["./scripts/testPreload.ts"]